/** Review-mode indicator: a marker-amber bar across the top of the paper/preview frame, shared by MdCanvas and HtmlCanvas. */
export function ReviewModeBar() {
  return <div className="h-0.5 shrink-0 bg-marker" aria-hidden />;
}
